# ============================================
# TrewMonitor - Environment Variables
# ============================================
# Copy this file to .env and fill in your values.
# The app runs in demo mode by default until exchange API keys
# are added through the Settings page in the app.

# ============================================
# Database (PostgreSQL)
# ============================================
POSTGRES_USER=trewmonitor
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=trewmonitor

# Full connection string - update with the values above
DATABASE_URL="postgresql://trewmonitor:your_secure_password_here@db:5432/trewmonitor?schema=public"

# ============================================
# NextAuth (Authentication)
# ============================================
# The full URL of your app (no trailing slash)
# Local dev: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=https://yourdomain.com

# Random secret used to sign session tokens
# Generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_here

# ============================================
# Encryption
# ============================================
# Used to encrypt exchange API keys stored in the database
# Generate one with: openssl rand -base64 32
ENCRYPTION_KEY=your_random_encryption_key_here
